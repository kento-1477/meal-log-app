#!/usr/bin/env node
/**
 * check_diffs.js
 * ----------------------------------
 * Placeholder that will later verify diff metrics against Spec §19 thresholds.
 * For now it fails with a clear message so the workflow blocks until implemented.
 */

console.error(
  'check_diffs.js is not implemented yet. Implement Spec §19 diff checks.',
);
process.exit(1);
